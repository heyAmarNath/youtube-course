"use client";
import { useState } from "react";
import { Clock3 } from "lucide-react";
const presets = [60, 120, 180, 240, 300, 360];
export function DailyTargetCard({ value, onChange }: { value: number; onChange: (minutes: number) => void }) { const [custom, setCustom] = useState(""); return <section className="rounded-xl border border-[#e6e5e0] bg-white p-6"><div className="flex items-center gap-3"><Clock3 className="h-5 w-5 text-[#f54e00]"/><div><h2 className="text-lg font-semibold">Daily learning target</h2><p className="text-sm text-[#807d72]">Set a pace that fits your day.</p></div></div><div className="mt-5 grid grid-cols-3 gap-2">{presets.map((minutes) => <button key={minutes} onClick={() => onChange(minutes)} className={`h-10 rounded-md border px-3 text-sm font-medium ${value === minutes ? "border-[#f54e00] bg-[#f54e00] text-white" : "border-[#cfcdc4] bg-white text-[#26251e]"}`}>{minutes / 60} hr</button>)}</div><form onSubmit={(e) => { e.preventDefault(); const minutes = Number(custom); if (minutes > 0) { onChange(minutes); setCustom(""); } }} className="mt-2 flex w-full"><input value={custom} onChange={(e) => setCustom(e.target.value)} inputMode="numeric" placeholder="Custom minutes" className="h-10 min-w-0 flex-1 rounded-l-md border border-[#cfcdc4] px-3 text-sm outline-none"/><button className="h-10 rounded-r-md border border-l-0 border-[#cfcdc4] px-3 text-sm">Set</button></form></section>; }
