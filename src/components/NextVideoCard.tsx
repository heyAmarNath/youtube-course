import { Play } from "lucide-react";
import { formatDuration } from "@/lib/duration";
import type { Video } from "@/types/youtube";
export function NextVideoCard({ video }: { video?: Video }) { if (!video) return null; return <section className="overflow-hidden rounded-xl border border-[#e6e5e0] bg-white"><div className="p-6"><p className="text-[11px] font-semibold uppercase tracking-[.88px] text-[#807d72]">Next up</p><div className="mt-4 flex gap-4"><img src={video.thumbnail.url} alt="" className="h-20 w-32 rounded-md object-cover"/><div className="min-w-0"><h2 className="line-clamp-2 text-lg font-semibold">{video.title}</h2><p className="mt-2 text-sm text-[#807d72]">{formatDuration(video.durationSeconds)}</p><a target="_blank" rel="noreferrer" href={`https://www.youtube.com/watch?v=${video.id}`} className="mt-3 inline-flex h-10 items-center gap-2 rounded-md bg-[#f54e00] px-4 text-sm font-medium text-white"><Play className="h-4 w-4 fill-current"/>Watch next</a></div></div></div></section>; }
