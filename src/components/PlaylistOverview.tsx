import { formatDuration } from "@/lib/duration";
import type { Playlist } from "@/types/youtube";
export function PlaylistOverview({ playlist, totalSeconds }: { playlist: Playlist; totalSeconds: number }) { return <section className="flex flex-col gap-5 rounded-xl border border-[#e6e5e0] bg-white p-6 sm:flex-row"><div className="aspect-video w-full shrink-0 overflow-hidden rounded-lg bg-[#efeee8] sm:w-80 lg:w-96">{playlist.thumbnail.url ? <img src={playlist.thumbnail.url} alt="" className="h-full w-full object-cover"/> : null}</div><div className="min-w-0 self-center"><h1 className="text-2xl font-normal tracking-tight sm:text-3xl">{playlist.title}</h1><p className="mt-3 text-sm font-medium text-[#807d72]">{playlist.channelTitle}</p><p className="mt-2 text-sm text-[#5a5852]">{formatDuration(totalSeconds)}</p></div></section>; }
